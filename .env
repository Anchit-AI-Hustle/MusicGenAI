# All API keys should be in Vercel Environment Variables only
# Do not store any tokens or secrets in this file

# This file should remain empty or only contain non-sensitive config
# All sensitive keys (API keys, tokens) must be in Vercel Dashboard → Settings → Environment Variables